import { expect } from 'chai';
import { unsubscribe } from './unsubscription';

describe(unsubscribe.name, () => {
  it('is the imperative shell that wires up the funtional core pieces', () => {
    // TODO
    expect(unsubscribe).to.exist;
  });
});
